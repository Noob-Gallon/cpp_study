#include <iostream>

using namespace std;

int main(void)
{
	/*
	Function Overloading

	- 같은 함수의 이름을 허용 (C언어에서는 불가능하다.)
	- 단, 매개변수가 개수 혹은 타입에서 달라야한다...

	void func_a(void)
	void func_a(int a)
	void func_a(float a)
	void func_a(float a, int b)
	void func_a(int a, flot b)

	- 주의, 함수 반환형은 상관없음.

	int func_a (int k)
	void func_a (int k)
	=> 허용되지 않는다.
	*/

	return 0;
}